version: "3.9"

services:
  api:
    build: .
    container_name: fastapi-gemini-api
    env_file:
      - .env
    ports:
      - "8000:8000"
    command: >
      sh -c "uvicorn app.main:app
      --host 0.0.0.0
      --port $${APP_PORT:-8000}
      --workers $${WEB_CONCURRENCY:-2}"
